---
phase: parkio-12-executable-workflow-diagram
plan: "01"
type: foundation
wave: 1
depends_on:
  - "11-01"
files_modified:
  - workflow/diagram/schema.json
  - workflow/diagram/README.md
  - workflow/diagram/daily-newsletter.graph.json
  - scripts/workflow_graph_lib.py
  - scripts/workflow_graph_validate.py
  - scripts/workflow_graph_dry_run.py
  - tests/test_workflow_graph.py
autonomous: true
requirements:
  - DG-01
  - DG-02
  - DG-03
  - DG-04
  - DG-05
user_setup: []
must_haves:
  truths:
    - Workflow graph is source of truth for dry-run order.
    - Failure-only nodes do not run on the normal dry-run path.
    - Production commands are not executed by the dry-run runner.
    - Edge changes affect execution order.
---

<objective>
Create the first executable workflow diagram graph for Daily Inbox.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Define workflow graph contract</name>
  <action>Add schema and README under `workflow/diagram/`.</action>
  <verify>python3 scripts/workflow_graph_validate.py</verify>
</task>

<task type="auto">
  <name>Task 2: Create Daily Inbox graph</name>
  <action>Add channel fetch, media enrichment, source health, digest aggregation, quality, finalize, status, and alert nodes.</action>
  <verify>python3 scripts/workflow_graph_dry_run.py</verify>
</task>

<task type="auto">
  <name>Task 3: Implement validator and dry-run runner</name>
  <action>Add workflow graph utilities, validator, dry-run command, and tests.</action>
  <verify>python3 tests/test_workflow_graph.py</verify>
</task>

</tasks>

<verification>
- [x] python3 -m py_compile scripts/workflow_graph_lib.py scripts/workflow_graph_validate.py scripts/workflow_graph_dry_run.py tests/test_workflow_graph.py
- [x] python3 scripts/workflow_graph_validate.py
- [x] python3 scripts/workflow_graph_dry_run.py
- [x] python3 tests/test_workflow_graph.py
</verification>

<success_criteria>

- DG-01 through DG-05 are satisfied.
- Normal dry-run prints execution order without running commands.
- Failure-only alert nodes are excluded from normal dry-run.
- Tests prove edge-order mutation behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/parkio-12-executable-workflow-diagram/12-01-SUMMARY.md`.
</output>

