---
phase: parkio-04-reader-quality-regression-lock
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_reader_quality_contract.py
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02
  - QUAL-03
  - QUAL-04
user_setup: []
must_haves:
  truths:
    - X title truncation and first-sentence duplication are covered by deterministic tests.
    - Promo, no-transcript, too-short, and summary-free media cannot enter the reader body.
    - Active Douyin sources are derived from `sources.md` through `digest_config.active_douyin_source_names()`.
    - Metadata leaks, transcript/status leaks, third-person narrator leakage, raw English prose, and Markdown/HTML divergence are covered by gates.
  artifacts:
    - tests/test_reader_quality_contract.py
  key_links:
    - Existing focused tests remain the detailed coverage.
    - The new contract test makes Phase 4 success criteria visible in one place.
---

<objective>
Lock the recent reader-facing quality fixes as a deterministic regression contract.

Purpose: The owner needs the daily digest to stay worth reading without repeated hand-fixes. This plan converts scattered quality protections into one Phase 4 contract test and updates planning state only after verification passes.

Output: A focused regression test, verification evidence, and planning state updates.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
@$HOME/.codex/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/parkio-04-reader-quality-regression-lock/04-CONTEXT.md
@summarize.py
@digest_config.py
@quality-check.py
@tests/test_titles.py
@tests/test_media.py
@tests/test_douyin_delivery.py
@HANDOVER.md
@GOTCHAS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add consolidated reader quality contract coverage</name>
  <files>tests/test_reader_quality_contract.py</files>
  <read_first>summarize.py, digest_config.py, quality-check.py, tests/test_titles.py, tests/test_media.py</read_first>
  <action>Add a deterministic test module that imports the existing guards and asserts the Phase 4 contract: chopped X titles are detected, media publishability rejects no-transcript and promo records while accepting deep summaries, active Douyin source names come from source configuration, and visible-product quality gates include the key bad patterns plus raw-English detection.</action>
  <verify>python3 tests/test_reader_quality_contract.py</verify>
  <acceptance_criteria>
    - Test is offline and does not edit `~/park-io/sources.md`.
    - Test does not call an LLM.
    - Test uses monkeypatch-free restoration or simple module-state restoration for cache changes.
  </acceptance_criteria>
  <done>Phase 4 success criteria are visible in one deterministic test file.</done>
</task>

<task type="auto">
  <name>Task 2: Patch only if the contract exposes a missing guard</name>
  <files>summarize.py, digest_config.py, quality-check.py</files>
  <read_first>tests/test_reader_quality_contract.py, summarize.py, digest_config.py, quality-check.py</read_first>
  <action>If the new test reveals a real missing quality guard, make the smallest production change required. If it passes because existing focused tests already cover the behavior, leave production code unchanged.</action>
  <verify>python3 tests/test_reader_quality_contract.py && python3 -m py_compile summarize.py digest_config.py quality-check.py</verify>
  <acceptance_criteria>
    - No new LLM path is introduced.
    - No generated daily artifacts are changed.
    - Existing source routing remains deterministic.
  </acceptance_criteria>
  <done>Reader quality contract passes with either existing or minimally tightened guards.</done>
</task>

<task type="auto">
  <name>Task 3: Complete Phase 4 planning state and verification</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</read_first>
  <action>Mark QUAL-01 through QUAL-04 complete and Phase 4 complete only after focused and full verification pass. Point state to Phase 5 as the next target.</action>
  <verify>for t in tests/test_*.py; do python3 "$t"; done && PARKIO_BATCH_ID=20260604 PARKIO_SKIP_AI_QUALITY=1 python3 check-quality.py</verify>
  <acceptance_criteria>
    - Phase 4 plan shows complete.
    - Phase 5 is the next current focus.
    - Summary file records verification evidence and residual risk.
  </acceptance_criteria>
  <done>Phase 4 is closed and ready for end-to-end daily run proof.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] python3 tests/test_reader_quality_contract.py
- [ ] python3 -m py_compile summarize.py digest_config.py quality-check.py
- [ ] for t in tests/test_*.py; do python3 "$t"; done
- [ ] PARKIO_BATCH_ID=20260604 PARKIO_SKIP_AI_QUALITY=1 python3 check-quality.py
- [ ] node /Users/wendy/.codex/gsd-core/bin/gsd-tools.cjs phase-plan-index 4
- [ ] node /Users/wendy/.codex/gsd-core/bin/gsd-tools.cjs verify-summary .planning/phases/parkio-04-reader-quality-regression-lock/04-01-SUMMARY.md .planning/phases/parkio-04-reader-quality-regression-lock/04-01-PLAN.md
</verification>

<success_criteria>

- X title truncation and first-sentence duplication are rejected or regenerated.
- Promo/no-transcript media cannot enter the reader body.
- Active Douyin sources are derived from `sources.md`, not a hardcoded whitelist.
- Metadata leaks, third-person narrator leakage, raw English body lines, and Markdown/HTML divergence are covered by tests or gates.
</success_criteria>

<threat_model>
This phase does not add credentials, external writes, or network calls. The main risk is a brittle test that overfits implementation details. Mitigation: assert public guard behavior and documented constants rather than entire rendered digest text.
</threat_model>

<output>
After completion, create `.planning/phases/parkio-04-reader-quality-regression-lock/04-01-SUMMARY.md`.
</output>
