---
phase: parkio-02-local-artifact-parity
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - finalize-local.py
  - tests/test_finalize_local.py
  - README.md
  - GOTCHAS.md
  - HANDOVER.md
autonomous: true
requirements:
  - ART-01
  - ART-02
  - ART-03
user_setup: []
must_haves:
  truths:
    - The local finalization stage copies Markdown, HTML, and PNG from the processed batch into sent when all three processed artifacts exist.
    - The PNG is copied from the processed artifact and is not regenerated by finalize-local.py.
    - Missing processed Markdown remains a hard failure.
    - Missing processed HTML or PNG produces a warning and does not hide the existing Markdown artifact.
  artifacts:
    - tests/test_finalize_local.py
    - finalize-local.py
  key_links:
    - finalize-local.py uses lib.batch_artifact_paths() to locate the processed Markdown, HTML, and PNG.
    - tests/test_finalize_local.py patches finalize-local.py paths instead of touching real ~/park-io data.
---

<objective>
Make local daily finalization preserve the full digest artifact family.

Purpose: The owner reads local artifacts, and the current handover says PNG derives correctly but only exists in the send stage. The normal local path should leave a complete `{date}.md`, `{date}.html`, `{date}.png` set under `~/park-io/inbox/sent/`.

Output: A focused regression test, a minimal `finalize-local.py` change, and docs updated to the new artifact contract.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
@$HOME/.codex/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/parkio-02-local-artifact-parity/02-CONTEXT.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/TESTING.md
@finalize-local.py
@build-digest.py
@lib.py
@README.md
@GOTCHAS.md
@HANDOVER.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add failing regression test for PNG finalization</name>
  <files>tests/test_finalize_local.py</files>
  <read_first>finalize-local.py, lib.py, tests/test_alerts.py, tests/test_llm_fallback.py</read_first>
  <action>Create `tests/test_finalize_local.py` as a standalone plain-Python test file. Load `finalize-local.py` with `importlib.util.spec_from_file_location` because its filename contains a hyphen. Use `tempfile.TemporaryDirectory()` to create a fake processed directory and fake sent directory. Write three source files named `000-26-06-04.md`, `000-26-06-04.html`, and `000-26-06-04.png` with distinct bytes. Patch the loaded module's `SENT_DIR`, `batch_label`, and `batch_artifact_paths` so `main()` uses the temp files. Assert `main()` returns 0 and the sent outputs `26-06-04.md`, `26-06-04.html`, and `26-06-04.png` all exist with matching content. Add a second test proving missing processed Markdown returns 1.</action>
  <verify>python3 tests/test_finalize_local.py should fail before the production change because PNG is not copied.</verify>
  <acceptance_criteria>
    - The test does not read or write real `~/park-io` paths.
    - The test does not require Chrome, Telegram, LLM keys, or live batches.
    - The test uses the repo's existing standalone runner style.
  </acceptance_criteria>
  <done>`tests/test_finalize_local.py` exists and demonstrates the missing PNG behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Copy processed PNG during local finalization</name>
  <files>finalize-local.py</files>
  <read_first>finalize-local.py, build-digest.py, lib.py</read_first>
  <action>Update `finalize-local.py` so `main()` destructures `panel, html, png = batch_artifact_paths()`. After copying Markdown and optional HTML, copy `png` to `SENT_DIR / f"{label}.png"` if it exists, using the existing `_finalize()` temp-copy helper. Print `[finalize-local] wrote {png_dst}` on success. If the processed PNG does not exist, print `[finalize-local] WARN missing processed png: {png}` to stderr and still return 0. Do not call `html-to-long-image.py`, do not call any LLM, and do not change `build-digest.py` unless the tests prove a hard need.</action>
  <verify>python3 tests/test_finalize_local.py && python3 -m py_compile finalize-local.py</verify>
  <acceptance_criteria>
    - Existing Markdown hard-failure behavior is preserved.
    - HTML copy behavior is preserved.
    - PNG copy behavior uses the same atomic temp replace helper as Markdown and HTML.
    - No second PNG render path is added.
  </acceptance_criteria>
  <done>`finalize-local.py` copies PNG when present and all focused tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile artifact documentation</name>
  <files>README.md, GOTCHAS.md, HANDOVER.md</files>
  <read_first>README.md, GOTCHAS.md, HANDOVER.md</read_first>
  <action>Update docs so they no longer claim PNG is only produced in the send stage or that sent keeps only Markdown/HTML. The target contract is: processed has `000-<date>.{md,html,png}` and local sent has `<date>.{md,html,png}` when the processed artifacts exist. In `GOTCHAS.md`, update the sent-artifact invariant to include HTML and PNG, while preserving the rule that Markdown is the content source.</action>
  <verify>rg -n "PNG|sent/|only|transient|send stage" README.md GOTCHAS.md HANDOVER.md</verify>
  <acceptance_criteria>
    - Documentation matches the implemented local artifact behavior.
    - Docs still say Markdown is the single content source.
    - Docs still say Telegram is disabled by default.
  </acceptance_criteria>
  <done>Artifact contract is consistent across README, GOTCHAS, and HANDOVER.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] python3 tests/test_finalize_local.py
- [ ] python3 -m py_compile finalize-local.py
- [ ] for t in tests/test_*.py; do python3 "$t"; done
- [ ] PARKIO_BATCH_ID=20260604 PARKIO_SKIP_AI_QUALITY=1 python3 check-quality.py
- [ ] git status --short shows only intentional changes before commit, then clean after commit
</verification>

<success_criteria>

- `~/park-io/inbox/sent/<date>.png` is produced by the normal local finalization path when processed PNG exists.
- Markdown remains the single content source; PNG derives from processed HTML and is only copied by finalization.
- The new test fails on the old behavior and passes after the implementation.
- README, GOTCHAS, and HANDOVER describe the same artifact contract.
</success_criteria>

<threat_model>
This phase does not add network calls, credential handling, or new external services. Primary risks are local file overwrite mistakes and accidental dependency on real user data in tests. Mitigation: use the existing atomic `_finalize()` helper, patch paths in tests, and avoid reading `~/park-io/secrets` or live inbox data.
</threat_model>

<output>
After completion, create `.planning/phases/parkio-02-local-artifact-parity/02-01-SUMMARY.md`.
</output>
