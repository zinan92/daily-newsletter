---
phase: parkio-09-aggregation-boundary-workflow-spec
plan: "01"
type: refactor
wave: 1
depends_on:
  - "08-01"
files_modified:
  - score.py
  - score-items.py
  - build-digest.py
  - summarize.py
  - check-quality.py
  - quality-check.py
  - ai-quality-check.py
  - archive-items.py
  - finalize-local.py
  - html-to-long-image.py
  - aggregation/digest/score_stage.py
  - aggregation/digest/score_items.py
  - aggregation/digest/build.py
  - aggregation/digest/summarize.py
  - aggregation/digest/check_stage.py
  - aggregation/digest/quality.py
  - aggregation/digest/ai_quality.py
  - aggregation/digest/archive.py
  - aggregation/digest/finalize_local.py
  - aggregation/digest/html_to_long_image.py
  - aggregation/digest/CONTRACT.md
  - workflow/daily-newsletter.workflow.yaml
  - tests/test_ingestion_wrappers.py
autonomous: true
requirements:
  - DEC-04
  - DEC-05
  - DEC-07
user_setup: []
must_haves:
  truths:
    - Digest aggregation implementation lives under `aggregation/digest/`.
    - Root commands remain working wrappers.
    - Root `summarize` import remains compatible for existing tests and tools.
    - Workflow spec shows ingestion, enrichment, aggregation, quality, and artifacts.
---

<objective>
Move digest aggregation into `aggregation/digest/` and keep the existing production command surface intact.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Move aggregation scripts</name>
  <action>Move score, score-items, build, summarize, quality, AI quality, archive, finalize, and HTML-to-image implementations into `aggregation/digest/` modules with repo-root bootstrap.</action>
  <verify>python3 -m py_compile aggregation/digest/*.py</verify>
</task>

<task type="auto">
  <name>Task 2: Preserve root compatibility</name>
  <action>Replace root aggregation scripts with wrappers that re-export functions and run the moved module `main()` or stage command.</action>
  <verify>python3 tests/test_ingestion_wrappers.py && python3 tests/test_finalize_local.py</verify>
</task>

<task type="auto">
  <name>Task 3: Update workflow-as-code</name>
  <action>Update `aggregation/digest/CONTRACT.md` and `workflow/daily-newsletter.workflow.yaml` with implementation paths and runtime flow.</action>
  <verify>python3 tests/test_ingestion_contracts.py</verify>
</task>

</tasks>

<verification>
- [ ] python3 -m py_compile score.py score-items.py build-digest.py summarize.py check-quality.py quality-check.py ai-quality-check.py archive-items.py finalize-local.py html-to-long-image.py aggregation/digest/*.py
- [ ] python3 tests/test_ingestion_wrappers.py
- [ ] python3 tests/test_finalize_local.py
- [ ] python3 tests/test_ingestion_contracts.py
- [ ] for t in tests/test_*.py; do python3 "$t"; done
- [ ] node /Users/wendy/.codex/gsd-core/bin/gsd-tools.cjs phase-plan-index 9
</verification>

<success_criteria>

- DEC-04 is satisfied for the digest aggregation boundary.
- DEC-05 remains true for aggregation root entrypoints.
- DEC-07 is strengthened by mapping implementation paths in workflow-as-code.
- No reader-facing digest behavior changes.
</success_criteria>

<output>
After completion, create `.planning/phases/parkio-09-aggregation-boundary-workflow-spec/09-01-SUMMARY.md`.
</output>

