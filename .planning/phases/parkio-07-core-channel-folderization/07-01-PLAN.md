---
phase: parkio-07-core-channel-folderization
plan: "01"
type: refactor
wave: 1
depends_on:
  - "06-01"
files_modified:
  - fetch-rss.py
  - fetch-scrape.py
  - fetch-twitter.py
  - fetch-twitter-saved.py
  - ingestion/rss/run.py
  - ingestion/web_scrape/run.py
  - ingestion/x/timeline.py
  - ingestion/x/saved.py
  - tests/test_ingestion_wrappers.py
autonomous: true
requirements:
  - DEC-01
  - DEC-05
user_setup: []
must_haves:
  truths:
    - Core channel code lives under channel folders.
    - Root entrypoints remain runnable wrappers.
    - Root wrappers re-export public helper functions for existing import-based tools.
    - Existing tests continue to pass.
---

<objective>
Folderize RSS, web scrape, and X ingestion behind compatibility wrappers.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Move core channel scripts</name>
  <action>Move RSS, web scrape, X timeline, and X saved scripts into their channel folders. Add repo-root sys.path bootstrap to moved modules so direct execution still works.</action>
  <verify>python3 -m py_compile fetch-rss.py fetch-scrape.py fetch-twitter.py fetch-twitter-saved.py ingestion/rss/run.py ingestion/web_scrape/run.py ingestion/x/timeline.py ingestion/x/saved.py</verify>
</task>

<task type="auto">
  <name>Task 2: Preserve root wrapper imports</name>
  <action>Replace root scripts with wrappers that import all public symbols from moved modules and call `main()` when executed.</action>
  <verify>python3 tests/test_ingestion_wrappers.py</verify>
</task>

<task type="auto">
  <name>Task 3: Verify affected tests</name>
  <action>Run scrape and wrapper tests plus full suite before completion.</action>
  <verify>python3 tests/test_scrape_sitemap.py && python3 tests/test_ingestion_contracts.py && python3 tests/test_ingestion_wrappers.py</verify>
</task>

</tasks>

<verification>
- [ ] python3 -m py_compile fetch-rss.py fetch-scrape.py fetch-twitter.py fetch-twitter-saved.py ingestion/rss/run.py ingestion/web_scrape/run.py ingestion/x/timeline.py ingestion/x/saved.py
- [ ] python3 tests/test_ingestion_wrappers.py
- [ ] python3 tests/test_scrape_sitemap.py
- [ ] for t in tests/test_*.py; do python3 "$t"; done
- [ ] node /Users/wendy/.codex/gsd-core/bin/gsd-tools.cjs phase-plan-index 7
</verification>

<success_criteria>

- DEC-01 and DEC-05 are satisfied for RSS, web scrape, and X channels.
- Root commands keep working.
- No reader-facing digest behavior changes.
</success_criteria>

<output>
After completion, create `.planning/phases/parkio-07-core-channel-folderization/07-01-SUMMARY.md`.
</output>
