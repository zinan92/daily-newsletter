---
phase: parkio-03-health-visibility-closure
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - channel-health.py
  - summarize.py
  - tests/test_channel_health.py
  - tests/test_health_dashboard.py
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - HLTH-01
  - HLTH-02
  - HLTH-03
user_setup: []
must_haves:
  truths:
    - Digest Markdown contains a compact `## 渠道概览` dashboard.
    - Pending WeWe RSS setup is not counted as a healthy channel.
    - A seed/manual WeChat article cannot mask an unconfigured automated WeWe feed.
    - Detailed per-source reasons remain available in status/local health surfaces.
  artifacts:
    - tests/test_channel_health.py
    - tests/test_health_dashboard.py
  key_links:
    - channel-health.py exposes pending WeWe setup as DOWN/action-needed.
    - summarize.source_health() honors pending setup before classifying WeChat seed content as ok_new.
---

<objective>
Close the health visibility gap for frozen and pending WeChat sources.

Purpose: The owner needs to know whether a quiet channel is truly quiet, frozen, failed, or not configured. `Ray在思考` is already stale; `克劳德猎手` must surface as pending WeWe setup rather than being hidden by a seed article.

Output: Health classification changes, focused tests, and planning state updates.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
@$HOME/.codex/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/parkio-03-health-visibility-closure/03-CONTEXT.md
@channel-health.py
@summarize.py
@check-pipeline-health.py
@tests/test_channel_health.py
@tests/test_health_dashboard.py
@HANDOVER.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pending WeWe setup regression coverage</name>
  <files>tests/test_channel_health.py, tests/test_health_dashboard.py</files>
  <read_first>channel-health.py, summarize.py, tests/test_channel_health.py, tests/test_health_dashboard.py</read_first>
  <action>Add tests proving a WeChat source row with notes containing `rss_url pending WeWe subscription` is action-needed rather than healthy/quiet. Add a dashboard/source-health test proving a `克劳德猎手`-style seed item does not hide pending setup and appears in the compact dashboard attention list.</action>
  <verify>python3 tests/test_channel_health.py && python3 tests/test_health_dashboard.py should fail before implementation.</verify>
  <acceptance_criteria>
    - Tests are deterministic and offline.
    - Tests do not edit `/Users/wendy/park-io/sources.md`.
    - Tests assert both low-level channel classification and digest-facing dashboard behavior.
  </acceptance_criteria>
  <done>Failing tests capture the hidden pending RSS setup bug.</done>
</task>

<task type="auto">
  <name>Task 2: Surface pending WeWe setup in health classification</name>
  <files>channel-health.py, summarize.py</files>
  <read_first>channel-health.py, summarize.py, check-pipeline-health.py</read_first>
  <action>Implement a pure pending-setup detector for active WeChat rows whose notes contain `rss_url pending`. In `channel-health.py`, classify those rows as `DOWN` with an actionable error message before log lookup. In `summarize.source_health()`, apply the same detector before `today_src` classification so seed/manual WeChat content cannot mark an unconfigured automated feed as `ok_new`. Keep `Ray在思考` stale behavior unchanged.</action>
  <verify>python3 tests/test_channel_health.py && python3 tests/test_health_dashboard.py</verify>
  <acceptance_criteria>
    - `克劳德猎手` appears as an attention-needed source when its RSS URL is pending.
    - Pending setup has a clear detail string mentioning WeWe RSS setup.
    - Existing health dashboard remains at 5 nonblank lines or fewer.
  </acceptance_criteria>
  <done>Pending WeWe setup is visible in channel health and digest health.</done>
</task>

<task type="auto">
  <name>Task 3: Update planning state and verify Phase 3</name>
  <files>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>.planning/PROJECT.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</read_first>
  <action>Mark Phase 3 requirements complete only after tests pass. Remove stale state text saying `GOTCHAS.md` still has old source-health wording. Keep owner/infra action for actually creating the WeWe subscription as unresolved.</action>
  <verify>python3 -m py_compile summarize.py channel-health.py check-pipeline-health.py && for t in tests/test_*.py; do python3 "$t"; done && PARKIO_BATCH_ID=20260604 PARKIO_SKIP_AI_QUALITY=1 python3 check-quality.py</verify>
  <acceptance_criteria>
    - Phase 3 state points to Phase 4 as next.
    - HLTH-01, HLTH-02, HLTH-03 are marked complete only if verification passes.
    - Repo status is clean after commit.
  </acceptance_criteria>
  <done>Phase 3 is completed and documented.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] python3 tests/test_channel_health.py
- [ ] python3 tests/test_health_dashboard.py
- [ ] python3 -m py_compile summarize.py channel-health.py check-pipeline-health.py
- [ ] for t in tests/test_*.py; do python3 "$t"; done
- [ ] PARKIO_BATCH_ID=20260604 PARKIO_SKIP_AI_QUALITY=1 python3 check-quality.py
- [ ] python3 channel-health.py --json shows `克劳德猎手` action-needed while `Ray在思考` remains stale
</verification>

<success_criteria>

- `## 渠道概览` remains compact and near the top of the digest.
- `克劳德猎手` pending WeWe RSS setup is visible as action-needed, not hidden by seed content.
- `Ray在思考` frozen feed remains visible as stale.
- Detailed per-source reasons remain available in status/local health outputs.
</success_criteria>

<threat_model>
This phase does not add credentials, network destinations, or external writes. The main risk is false-red health for valid WeChat sources. Mitigation: trigger pending setup only when source notes explicitly contain `rss_url pending`, leaving configured RSS sources and manual seed imports otherwise unchanged.
</threat_model>

<output>
After completion, create `.planning/phases/parkio-03-health-visibility-closure/03-01-SUMMARY.md`.
</output>
